[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra
markers =
    unit: pure Python tests without external services
    integration: tests requiring project runtime dependencies
    live_llm: tests requiring a real LLM API
